{
  "name": "encoding-proxy",
  "version": "1.0.0",
  "description": "Proxy for converting binary body payload (a buffer) to string with custom encoding. If encoding is not specified, it doesn't convert a buffer to string.",
  "main": "./src/encoding-proxy.js",
  "bin": {
    "encoding-proxy": "./bin/encoding-proxy"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "bin/encoding-proxy"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mrac/encoding-proxy.git"
  },
  "keywords": [
    "encoding",
    "proxy",
    "server"
  ],
  "author": "Michal Raczkowski",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/mrac/encoding-proxy/issues"
  },
  "homepage": "https://github.com/mrac/encoding-proxy#readme",
  "dependencies": {
    "request": "^2.85.0"
  }
}
